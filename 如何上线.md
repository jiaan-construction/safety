---
title: 上线说明｜塔吊司机安全须知与确认书
type: guide
created: 2026-08-21
status: 待执行（需用户本人操作，AI 不代传外部服务）
---

# 如何上线

本目录只有一个文件：`index.html`（159 KB，单文件，零外部依赖）。
把它放到任何静态托管上就能用，工人扫码或点链接即可打开。

---

## 一、为什么必须托管

| 发送方式 | 结果 |
|---|---|
| WhatsApp 发 `.html` 文件 | ❌ 打不开。WhatsApp 不预览 HTML，工人只看到「无法预览」 |
| 微信发 `.html` 文件 | ⚠️ 要点「用其他应用打开」，多数工人到这步就放弃 |
| AirDrop / 邮件附件 | ✅ 能开，但一个个发不现实 |
| **托管后发链接** | ✅ 点开即用，可做二维码贴驾驶室 |

本文件是 **Level 1 公司级**文件，全公司通用，**一条链接发给所有司机**，不需要按工地生成不同链接。
（工地绑定属于 Level 2 的 `JAC-CR-SAF-004` 现场确认书，是另一份，尚未制作。）

---

## 二、推荐做法：Netlify Drop（最快，约 1 分钟）

不用注册也能先试，不用装任何东西。

1. 电脑浏览器打开 **https://app.netlify.com/drop**
2. 把本目录**整个文件夹**拖进网页中间的方框
3. 等几秒，页面会给出一条网址，形如 `https://xxxx-yyyy.netlify.app`
4. 手机打开这条网址验证一次，走通「填写 → 确认 → 发送回执」
5. 想固定网址和防止过期，注册免费账号后在 Site settings 里改站点名，例如
   `jiaan-crane-safety.netlify.app`

> 拖**文件夹**而不是单个文件，Netlify 会自动把 `index.html` 作为首页，网址最干净。
> 本 `如何上线.md` 一起被上传也无妨（不会被访问到），介意的话拖之前先把它移走。

**其他等价选择**：Cloudflare Pages、Vercel、GitHub Pages。都可以，Netlify Drop 步骤最少。
⚠️ 本库禁止 `git init`，所以 GitHub Pages 要另建仓库，不要在本库内做。

---

## 三、隐私：托管是安全的

**页面是纯静态的，工人填的姓名、电话、FIN 不会发给托管商。**

所有处理都在工人手机本地完成：填写、确认、生成回执、画成图片，全程没有任何网络请求
（文件里外部请求数 = 0，可自行搜索 `https://` 验证）。托管商只是把这个 HTML 发给浏览器，
拿不到任何输入内容。

回执只走工人主动的那一次分享（WhatsApp 或邮件），直接到公司，不经第三方。

---

## 四、上线后要做的事

- [ ] 手机实测一遍全流程（尤其在 WhatsApp 内置浏览器里打开测一次）
- [ ] 用任意二维码工具把网址做成二维码，打印贴到**驾驶室**和**工地办公室**
- [ ] 把链接发到工头群，说明「新司机入职、以及每年一次，都要走一遍」
- [ ] 建立归档规则：回执按 `确认编号 / 姓名 / 日期` 收进 `05-员工档案` 对应员工卡旁

## 五、还没做的（不要当成已完成）

- **没有后端**。回执只靠工人主动发回来。要做到「几分钟调出某人某版确认记录」，需要服务端落库，
  IP／设备／签发人／文本快照哈希也要那时才能写。
- **许俊尚未批准放行 Rev.01**，尤其 5.5 与 6.4 两条「不处罚」承诺。
- **Task 6 合规核查未做**（WSH / MOM / 电子签名法 / PDPA、保存年限）。生效日写的是 2026-09-01。
- **Level 2 现场确认书（JAC-CR-SAF-004）与 Level 3 每日确认（005）未制作。**
