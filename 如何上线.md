---
title: 上线说明｜塔吊司机安全须知与确认书
type: guide
created: 2026-08-21
updated: 2026-08-21
status: 已上线 → https://jiaan-construction.github.io/safety/
---

# 如何上线

**已经上线了**（V1.1，2026-08-21：手写签名 + 记录自动落库 + 收集告知），
正式网址：**https://jiaan-construction.github.io/safety/**
仓库 `jiaan-construction/safety`，GitHub Pages。旧地址 `keepmind9697.github.io/jiaan-crane-safety/`
已改成跳转页，只为接住已经发出去的旧链接，不要再从那边改内容。

本目录的 `index.html`（单文件，零外部依赖）是**唯一维护版本**。
改它 → 用 `gh api ... contents/index.html -X PUT` 覆盖上传。仓库是发布副本，不要反向改回来。

下面第二节保留作为「换托管商时怎么办」的备用说明。

---

## 一、为什么必须托管

| 发送方式 | 结果 |
|---|---|
| WhatsApp 发 `.html` 文件 | ❌ 打不开。WhatsApp 不预览 HTML，工人只看到「无法预览」 |
| 微信发 `.html` 文件 | ⚠️ 要点「用其他应用打开」，多数工人到这步就放弃 |
| AirDrop / 邮件附件 | ✅ 能开，但一个个发不现实 |
| **托管后发链接** | ✅ 点开即用，可做二维码贴驾驶室 |

本文件是 **Level 1 公司级**文件，全公司通用，**一条链接发给所有司机**，不需要按工地生成不同链接。
（工地绑定属于 Level 2 的 `JAC-CR-SAF-004` 现场确认书，是另一份，尚未制作。）

---

## 二、推荐做法：Netlify Drop（最快，约 1 分钟）

不用注册也能先试，不用装任何东西。

1. 电脑浏览器打开 **https://app.netlify.com/drop**
2. 把本目录**整个文件夹**拖进网页中间的方框
3. 等几秒，页面会给出一条网址，形如 `https://xxxx-yyyy.netlify.app`
4. 手机打开这条网址验证一次，走通「填写 → 确认 → 发送回执」
5. 想固定网址和防止过期，注册免费账号后在 Site settings 里改站点名，例如
   `jiaan-crane-safety.netlify.app`

> 拖**文件夹**而不是单个文件，Netlify 会自动把 `index.html` 作为首页，网址最干净。
> 本 `如何上线.md` 一起被上传也无妨（不会被访问到），介意的话拖之前先把它移走。

**其他等价选择**：Cloudflare Pages、Vercel、GitHub Pages。都可以，Netlify Drop 步骤最少。
⚠️ 本库禁止 `git init`，所以 GitHub Pages 要另建仓库，不要在本库内做。

---

## 三、数据与隐私：现状（2026-08-21 后端已接通）

签署后，下列资料会提交到 JIA AN 在**新加坡区（ap-southeast-1）**的 Supabase 项目 `jiaan-safety`：

姓名、联系电话、**完整 FIN / 工准证号**、手写签名图、确认编号、文件编号与版本、
签署时间、8 节确认状态、条款全文 SHA-256、阅读时长。

- 页面上已加**收集告知**（Part A 顶部，中英双语：记录什么、存在哪、用途、查询与更正联系方式）。
- 页面只持有 Supabase 的 **publishable key**。数据库开了行级安全：该 key **只能新增，不能读、改、删**；
  签名桶私有，只允许写入 `ack/<确认编号>.png` 这一种路径，已存在的签名覆盖不了。全部实测验证过。
- 除本公司的 Supabase 外，页面没有任何第三方请求：不引 CDN、不用 SDK、无字体外链、无统计。
- 提交失败（工地没信号）不阻断回执：页面明确显示「公司系统未收到」并给重试按钮，回执照常能发。
- 不采集 IP、设备信息。

🔴 **仍未做**：PDPA 保存年限未设定；Task 6 合规核查（WSH / MOM / ETA / PDPA）未完成。
建库脚本与教程见 `08_Supabase后端/`。

---

## 四、上线后要做的事

- [ ] 手机实测一遍全流程（尤其在 WhatsApp 内置浏览器里打开测一次）
- [x] 二维码已生成：`07_二维码/QR_塔吊安全须知.svg`（印刷用矢量母版）与 `_1215px.png`，
      打印稿 `07_二维码/打印稿_二维码张贴与驾驶室贴纸.html`（A4 海报 1 张 + 驾驶室贴纸 4 张）
- [ ] 打印贴到**驾驶室**和**工地办公室**
- [ ] 把链接发到工头群，说明「新司机入职、以及每年一次，都要走一遍」
- [ ] 建立归档规则：回执按 `确认编号 / 姓名 / 日期` 收进 `05-员工档案` 对应员工卡旁

## 五、还没做的（不要当成已完成）

- **没有公司后台界面**。记录已自动落库，但查数据要登 Supabase 面板，没有按工地／按月的报表。
- **没有速率限制**。publishable key 是公开的，格式约束挡得住乱码，挡不住格式正确的伪造记录。
- **确认编号由手机签发**，是归档把手不是防伪凭证。权威时间以服务器 `created_at` 为准。
- **许俊尚未批准放行 Rev.01**，尤其 5.5 与 6.4 两条「不处罚」承诺。
- **Task 6 合规核查未做**（WSH / MOM / 电子签名法 / PDPA、保存年限）。生效日写的是 2026-09-01。
- **Level 2 现场确认书（JAC-CR-SAF-004）与 Level 3 每日确认（005）未制作。**
